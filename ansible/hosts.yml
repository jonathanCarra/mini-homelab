---
servers:
  hosts:
    docker-standalone:
      ansible_host: 192.168.1.145 # l'ip de votre serveur ubuntu 24.04
      ansible_user: ubuntu # Remplacez par votre utilisateur sudo
      ansible_ssh_private_key_file: ~/.ssh/id_rsa # chemin de votre clé sur la machine local
